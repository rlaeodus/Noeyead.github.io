// const images = [ "0.jpg", "1.jpg",]; // 이미 배열로 저장
// const chosenImage = images[Math.floor(Math.random() * images.length)]; // 배열 수 대로 랜덤수

// const bgImage = document.createElement("img"); //heml에 img 요소 추가

// bgImage.src = `img/${chosenImage}`; // 랜덤으로 배경 이미지 설정

// document.body.appendChild(bgImage); // bgImage body태그에 추가

